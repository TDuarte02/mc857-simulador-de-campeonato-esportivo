package com.mc857.copaamerica.nav

/** Screens implemented for the Modo Clássico flow (+ Home). Mirrors the `Screen` union in App.tsx:4, trimmed to this pass's scope. */
enum class Screen {
    HOME,
    TEAM_SELECTION,
    SQUAD_BUILDER,
    DRAW_SETUP,
    PRE_MATCH,
    MATCH_RESULT,
    GROUP_STAGE,
    BRACKET,
    ELIMINATION,
    PHASE_OVERVIEW,
}
